<!-- ============================================ -->
<!--                    CTA                       -->
<!-- ============================================ -->

<section id="cta-home">
    <div class="cs-container">
        <div class="cs-content">
            <span class="cs-topper">Ready to Stock Up?</span>
            <h2 class="cs-title">Get the Supplies Your Restaurant Needs, When You Need Them</h2>
            <p class="cs-text">
                Browse our full catalogue of restaurant disposables or reach out for bulk pricing and custom orders. Edmonton-based, fast fulfillment.
            </p>
            <div class="cs-button-group">
                <a href="/products" class="cs-button-solid">Shop Now</a>
                <a href="/contact" class="cs-button-outline">Contact Us</a>
            </div>
        </div>
        <!-- <ul class="cs-stat-group">
            <li class="cs-stat">
                <span class="cs-number">20+</span>
                <span class="cs-desc">Years in Business</span>
            </li>
            <li class="cs-divider" aria-hidden="true"></li>
            <li class="cs-stat">
                <span class="cs-number">100+</span>
                <span class="cs-desc">Products Available</span>
            </li>
            <li class="cs-divider" aria-hidden="true"></li>
            <li class="cs-stat">
                <span class="cs-number">YEG</span>
                <span class="cs-desc">Edmonton Local</span>
            </li>
        </ul> -->
    </div>
</section>

<style>
/*-- -------------------------- -->
<---            CTA             -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #cta-home {
        padding: var(--sectionPadding);
        background-color: #001f3f;
        position: relative;
        overflow: hidden;
    }
    /* Decorative background accent */
    #cta-home::before {
        content: "";
        width: 32rem;
        height: 32rem;
        background: var(--primary);
        opacity: 0.08;
        border-radius: 50%;
        position: absolute;
        top: -10rem;
        right: -8rem;
        pointer-events: none;
    }
    #cta-home::after {
        content: "";
        width: 20rem;
        height: 20rem;
        background: var(--secondary);
        opacity: 0.06;
        border-radius: 50%;
        position: absolute;
        bottom: -8rem;
        left: -4rem;
        pointer-events: none;
    }
    #cta-home .cs-container {
        width: 100%;
        max-width: 80rem;
        margin: auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: clamp(2.5rem, 5vw, 3.5rem);
        position: relative;
        z-index: 1;
    }
    #cta-home .cs-content {
        text-align: center;
        width: 100%;
        max-width: 72rem;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    #cta-home .cs-topper {
        color: var(--secondary);
    }
    #cta-home .cs-title {
        color: #fff;
        margin: 0 0 1rem;
        max-width: 72rem;
    }
    #cta-home .cs-text {
        color: rgba(255, 255, 255, 0.75);
        margin-bottom: 2rem;
        max-width: 56rem;
    }
    #cta-home .cs-button-group {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
    }
    #cta-home .cs-button-solid {
        font-size: 1rem;
        line-height: clamp(2.875rem, 5.5vw, 3.5rem);
        text-decoration: none;
        font-weight: 700;
        text-align: center;
        margin: 0;
        color: #fff;
        min-width: 9.375rem;
        padding: 0 2rem;
        background-color: var(--primary);
        border-radius: 0.25rem;
        display: inline-block;
        position: relative;
        z-index: 1;
        box-sizing: border-box;
        transition: background-color 0.3s;
    }
    #cta-home .cs-button-solid:before {
        content: "";
        position: absolute;
        height: 100%;
        width: 0%;
        background: #000;
        opacity: 0.3;
        top: 0;
        left: 0;
        z-index: -1;
        border-radius: 0.25rem;
        transition: width 0.3s;
    }
    #cta-home .cs-button-solid:hover:before {
        width: 100%;
    }
    #cta-home .cs-button-outline {
        font-size: 1rem;
        line-height: clamp(2.875rem, 5.5vw, 3.5rem);
        text-decoration: none;
        font-weight: 700;
        text-align: center;
        margin: 0;
        color: #fff;
        min-width: 9.375rem;
        padding: 0 2rem;
        background-color: transparent;
        border: 2px solid rgba(255, 255, 255, 0.4);
        border-radius: 0.25rem;
        display: inline-block;
        box-sizing: border-box;
        transition: border-color 0.3s, background-color 0.3s;
    }
    #cta-home .cs-button-outline:hover {
        border-color: #fff;
        background-color: rgba(255, 255, 255, 0.1);
    }
    /* Stats bar */
    #cta-home .cs-stat-group {
        list-style: none;
        width: 100%;
        max-width: 36rem;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        gap: clamp(1.5rem, 4vw, 2.5rem);
        border-top: 1px solid rgba(255, 255, 255, 0.12);
        padding-top: clamp(2rem, 4vw, 2.5rem);
    }
    #cta-home .cs-stat {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.25rem;
    }
    #cta-home .cs-number {
        font-size: clamp(1.75rem, 4vw, 2.5rem);
        font-weight: 900;
        line-height: 1em;
        color: var(--secondary);
    }
    #cta-home .cs-desc {
        font-size: clamp(0.75rem, 1.5vw, 0.875rem);
        line-height: 1.4em;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: rgba(255, 255, 255, 0.55);
        text-align: center;
    }
    #cta-home .cs-divider {
        width: 1px;
        height: 2.5rem;
        background-color: rgba(255, 255, 255, 0.15);
        flex-shrink: 0;
    }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #cta-home .cs-stat-group {
        max-width: none;
        gap: clamp(2.5rem, 6vw, 4rem);
    }
}
</style>
